local lib = require(game.ReplicatedStorage.Library)

local fromPet = "Cat"
local toPet = "Titanic Monkey"

for i,v in pairs(lib.Directory.Pets[Cat]) do
  lib.Directory.Pets[Cat][i] = nil
end
for i,v in pairs(lib.Directory.Pets[Huge Cat]) do
  lib.Directory.Pets[Huge Cat][i] = v
end
